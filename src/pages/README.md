# Pages

Add new pages here:
1) Create a new folder under `src/pages/<page>` with `<PageName>Page.tsx`.
2) Reuse translations from `src/app/i18n/resources.ts` or add new keys there.
3) Register the route in `src/app/router/index.tsx`.
